{
  "name": "partsindex",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Parts pricing reference for SG motor third-party claims — fuzzy-matched median benchmark from supplier bills.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_BASE=/partsindex/ vite build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "gh-pages": "^6.1.1",
    "vite": "^5.4.0"
  }
}
